#!/bin/sh
# managed-by: contrib-pilot
# Optional fast-tier check before push. Bypassable with `git push --no-verify`;
# CI remains authoritative regardless.
set -e
exec contrib-pilot validate --tier fast
